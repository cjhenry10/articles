---
title: 'Component Test'
date: '2023-05-09'
tags: ['nextjs', 'next.js']
---

### test
<Codeblock fileName='testing.js' code={`let one = 1;
let two = 'two';
let three = 3;
let four = 2 + 2;
let five = 7 - 2;`}>
```js /two/
let one = 1;
let two = 'two';
let three = 3;
let four = 2 + 2;
let five = 7 - 2;
```
</Codeblock>
<Codeblock fileName='style.css' code={`h1 {
  color: white;
  background-color: black;
}`}>
```css
h1 {
  color: white;
  background-color: black;
}
```
</Codeblock>
<Codeblock fileName='index.html' code={`<div>
  <h1>Hello there</h1>
</div>`}>
```html
<div>
  <h1>Hello there</h1>
</div>
```
</Codeblock>


### rehype prism plus
```js {1,3-4} showLineNumbers
let one = 1;
let two = 'two';
let three = 3;
let four = 2 + 2;
let five = 7 - 2;
```

```html {+3, +4, -5} showLineNumbers
<body>
  <div>
    <h1>Hello there</h1>
  </div>
</body>
```

### table test

| Syntax | Description |
| --- | ----------- |
| Header | Title |
| Paragraph | Text |

---

|Goal | Method |
| --- | ----- |
| Create an HTML element | `document.createElement('div');{:js}` |
| Get an HTML element | `document.getElementById('your-element-id');{:js}` |

### link test

My favorite search engine is [Duck Duck Go](https://duckduckgo.com "The best search engine for privacy").

### inline code test

At the command prompt, type `nano{:bash}`.

To create a variable storing a number, try this `let x = 15;{:js}`

### definition list

First Term
: This is the definition of the first term.

Second Term
: This is one definition of the second term.
: This is another definition of the second term.