---
title: 'DOM Cheatsheet'
subtitle: 'Some examples of how to use the Document Object Model'
date: '2023-05-10'
tags: ['javascript', 'DOM']
---

The DOM, or Document Object Model, provides methods to add, change, and remove HTML elements.

# Prerequisites
- HTML Elements
- Variables
- Strings
- Objects

# Cheatsheet
|Goal | Method |
| --- | ----- |
| Create | `document.createElement('div');{:js}` |
| Select | `document.getElementById('your-element-id');{:js}` |
| Add a class | `yourHtmlElement.classList.add('your-class');{:js}` |
| Remove a class | `yourHtmlElement.classList.remove('your-class');{:js}` |
| Add to the page | `yourParentElement.appendChild(yourHtmlElement);{:js}` |
| Remove from the page | `yourParentElement.removeChild(yourHtmlElement);{:js}` |
| Adjust the inner HTML | `yourHtmlElement.innerHTML = 'Your new text';{:js}` |

# Create an HTML Element
- code
  - `document.createElement(placeholder);{:js}`
- placeholder
  - replace with an HTML tag as a string, such as `'div'{:js}`, `'img'{:js}`, or `'section'{:js}`

### Examples
The code below creates a new `div{:js}` element and stores it in a variable named `newElement{:js}`. The element is not on the page yet.
<Codeblock fileName='script.js' code={`const newElement = document.createElement('div');
`}>
```js
const newElement = document.createElement('div');
```
</Codeblock>

The code below creates a new `p{:js}` element and stores it in a variable named `newParagraph{:js}`. The element is not on the page yet.
<Codeblock fileName='script.js' code={`const newElement = document.createElement('div');
`}>
```js
const newParagraph = document.createElement('p');
```
</Codeblock>

# Select an HTML element
- code
  - `document.getElementById(placeholder);{:js}`
- placeholder
  - replace with a string that matches the id of the element you want to select, such as `'about'{:js}` or `'menu-item'{:js}`

### Examples
The code below is part of the HTML file.
<Codeblock fileName='index.html' code={`<body>
  <div id="about-section">
  
  </div>
</body>`}>
```html
<body>
  <div id="about-section">

  </div>
</body>
```
</Codeblock>
The code below selects this `div` element and stores it in a variable named `about`.
<Codeblock fileName='script.js' code={`const about = document.getElementById('about-section');`}>
```js
const about = document.getElementById('about-section');
```
</Codeblock>